"""Phoenix operator web UI assets (Phase 13 Step 5c).

Holds the static control-panel PWA under ``static/`` served by
:mod:`phoenix.api.cognition_ui`. Not importable application code — the data
files ship in the wheel via ``[tool.setuptools.package-data]``.
"""
